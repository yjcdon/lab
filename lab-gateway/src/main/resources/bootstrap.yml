server:
  port: 9000

spring:
  application:
    name: lab-gateway # 服务名称
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.88.137:8848 # nacos地址
      config:
        server-addr: 192.168.88.137:8848
        file-extension: yml
        prefix: ${spring.application.name}
        shared-configs: common.yml,lab-feign.yml